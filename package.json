{
  "name": "mon-projet",
  "version": "1.0.0",
  "scripts": {
     "start": "concurrently \"npm run server\" \"npm run client\"",
     "server": "cd serveur && nodemon server",
     "client": "cd client && npm start"
  },
  "devDependencies": {
     "concurrently": "^7.0.0"
  }
}
